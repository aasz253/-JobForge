[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jobforge-email"
version = "0.1.0"
description = "Email intelligence for JobForge (Sifuna Codex)"
requires-python = ">=3.11"
dependencies = ["jobforge-shared>=0.1.0"]

[tool.setuptools]
packages = ["jf_email"]